// Stub for web-worker module that elkjs tries to require
// In browser environments, elkjs will use native Worker instead
// This prevents build errors when the web-worker package is not installed
export default {}
